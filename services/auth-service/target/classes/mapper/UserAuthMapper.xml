<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alvis.exam.auth.repository.UserAuthMapper">
  <resultMap id="UserResultMap" type="com.alvis.exam.auth.domain.User">
    <id column="id" property="id"/>
    <result column="user_name" property="userName"/>
    <result column="password" property="password"/>
    <result column="real_name" property="realName"/>
    <result column="role" property="role"/>
    <result column="status" property="status"/>
    <result column="user_uuid" property="userUuid"/>
  </resultMap>

  <select id="findByUsername" resultMap="UserResultMap">
    SELECT id, user_name, password, real_name, role, status, user_uuid
    FROM t_user WHERE user_name = #{username}
  </select>

  <select id="findByUsernamePwd" resultMap="UserResultMap">
    SELECT id, user_name, password, real_name, role, status, user_uuid
    FROM t_user
    WHERE user_name = #{username} AND password = #{pwd}
  </select>
</mapper>
